# Mobile HTML QA

Check:

- responsive layout;
- readable text on phone;
- sticky/horizontal navigation if used;
- no broken anchors;
- buttons clickable/tappable;
- back buttons in nested pages;
- no decorative overlay blocking taps;
- tables scroll horizontally on mobile;
- if iframe used, height is enough and not clipped.
