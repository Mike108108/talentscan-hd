/**
 * Re-exports BodyGraph Template v2 geometry.
 * Channel paths live in bodygraphTemplate.ts (fixed SVG, not computed).
 */
export {
  ALL_GATES,
  BODYGRAPH_VIEWBOX,
  CENTER_DEFINED_COLORS,
  CENTER_DEFINED_STROKES,
  CENTER_ORDER,
  CENTER_SHAPES,
  CHANNELS,
  CHANNEL_TEMPLATE,
  GATE_LABEL_POSITIONS,
  GATE_POSITIONS,
  type CenterKey,
  type CenterShape,
  type ChannelTemplateEntry,
  type Point,
} from "./bodygraphTemplate";
